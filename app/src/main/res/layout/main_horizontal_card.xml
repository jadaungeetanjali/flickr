<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/detail_movie_review"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#1c1c1c"
    android:paddingBottom="20dp"
    android:paddingTop="15dp"
    android:layout_marginBottom="20dp"
    app:layout_constraintTop_toBottomOf="@+id/detail_movie_similar">

    <TextView
        android:id="@+id/main_horizontal_card_heading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingBottom="15dp"
        android:paddingLeft="10dp"
        android:text="Reviews"
        android:textColor="#fff"
        android:textSize="22sp"
        app:layout_constraintTop_toBottomOf="parent" />

    <TextView
        android:id="@+id/main_horizontal_card_Button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginRight="15dp"
        android:text="MORE"
        android:textColor="#00a99d"
        android:padding="5dp"
        app:layout_constraintRight_toRightOf="parent"
        android:textSize="16sp" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/main_child_recyclerView"
        android:layout_width="match_parent"
        android:layout_height="230dp"
        android:background="#000"
        app:layout_constraintTop_toBottomOf="@+id/main_horizontal_card_heading"></android.support.v7.widget.RecyclerView>
</android.support.constraint.ConstraintLayout>
